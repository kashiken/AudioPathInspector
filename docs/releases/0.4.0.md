# Audio Path Inspector v0.4.0

This is a pre-release build focused on making the richer inspection results easier to navigate and on surfacing registry-derived APO evidence in the inferred chain.

## Highlights

- Added copyable plain-text diagnostic output from the current details tree and log.
- Added a filter box for the inspection details tree.
- Added context tooltips for inspection tree items, including APO, registry, HRESULT, and audiodg.exe diagnostics.
- Added Registry FX / Software Component APO candidates to `APO Chain (inferred)` as registry-derived chain hints.
- Collapsed noisy non-candidate sections by default, including raw registry evidence and full property/module dumps.

## Included from earlier releases

- Capture / Render endpoint switching.
- Registered APO inspection from endpoint properties.
- Inferred APO chain display grouped by Stream, Mode, and Endpoint effects.
- Registry evidence collection from MMDevices, PnP device, and endpoint property CLSID registry paths.
- Registry FX / Software Component APO candidate display.
- Endpoint property store dump with audio/FX/APO related property grouping.
- Windows Audio Effects inspection through `IAudioEffectsManager`.
- Audio Enhancements state detection from endpoint properties.
- Shared, Shared RAW, and Exclusive WASAPI open tests.
- APO notification interface support diagnostics.
- audiodg.exe loaded DLL diagnostics as supporting runtime evidence.
- Device enumeration and endpoint inspection work runs off the GUI thread.

## Notes

- `APO Chain (inferred)` remains a best-effort inference, not a guaranteed runtime execution trace.
- Registry-derived chain candidates are evidence-backed hints from `FxProperties` and Software Component registrations. They may represent vendor/private processing that is not exposed through standard `PKEY_FX_*` endpoint properties.
- Endpoint property names and values are best-effort diagnostics; unknown property keys are still shown by raw key.
- Registry evidence is read-only and may be incomplete when Windows denies access or expected registry paths are absent.
- Windows Audio Effects results only reflect effects reported by Windows APIs. Hardware DSP, USB device DSP, vendor-private processing, and driver-internal processing may not be visible.
- Some endpoints may not support `IAudioEffectsManager`, Shared RAW mode, or exclusive mode. In those cases the app displays the HRESULT or diagnostic message.
- audiodg.exe module enumeration may require sufficient process access and can fail on some systems; failures are shown as diagnostics.

## Artifact

Attach `AudioPathInspector-v0.4.0-win-x64.zip` to this GitHub Release.
